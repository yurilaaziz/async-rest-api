{
  "ignore_result": false,
  "broker": {
    "username": "barberousse",
    "password": "barberousse",
    "host": "localhost",
    "port": 5673,
    "protocol": "amqp",
    "broker_url": "{protocol}://{username}:{password}@{host}:{port}"
  },
  "routes": {
    "barberousse.worker.Executor": {
      "queue": "tasks"
    }
  }
}